# 설치 잠금 파일·시드 데이터 직접 접근 차단
<FilesMatch "^(\.lock|seed-listings\.json)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>
